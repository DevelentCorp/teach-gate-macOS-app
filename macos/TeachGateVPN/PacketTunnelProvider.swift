// Copyright 2024 Teach Gate
// VPN Extension placeholder. The real PacketTunnelProvider implementation
// is provided by OutlineVPNExtensionLib (ObjC) and referenced via Info.plist
// as 'PacketTunnelProvider'. This file must not declare a class with that
// name to avoid symbol conflicts. It exists only to keep the target with
// at least one Swift source and to aid future customizations if needed.

import Foundation

@objc(TeachGateVPNPlaceholder)
final class TeachGateVPNPlaceholder: NSObject {}